// build.gradle

plugins {
    id 'java'
}

group = 'com.eventstream'

version = '1.0'

repositories {
    mavenCentral()
}

sourceSets {
    main {
        java {
            srcDirs = ['broker', 'consumer', 'producer', 'shared']
        }
    }
}